package core;

public class Main {
    /**
     * Entry point of the application. Handles user interaction, command-line
     * parsing,
     * and coordinates the workflow between other classes.
     */
    public static void main(String[] args) {
    }
}